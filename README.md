# Pump It Up! 🚀

A fun and interactive click speed game where players try to click as fast as possible to raise a thermometer. The faster you click, the higher the temperature goes!

## Features

- **Interactive Thermometer**: Visual thermometer that fills up based on click speed
- **Real-time Speed Tracking**: Shows clicks per second as you click
- **Statistics**: Tracks total clicks and peak speed
- **Smooth Animations**: Beautiful, responsive UI with smooth transitions
- **Keyboard Support**: Press Spacebar to click (desktop)

## How to Play

1. Click the "Pump It Up!" button as fast as you can
2. Watch the thermometer rise as your click speed increases
3. Try to achieve your highest click speed!
4. Use the Reset button to start over

## Local Development

```bash
# Install dependencies (if any)
npm install

# Run development server with Vercel CLI
npx vercel dev

# Or use any static server
npx serve
```

## Deployment to Vercel

1. Push your code to GitHub:
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git remote add origin https://github.com/jmbluhm/example_repo_deployment.git
   git push -u origin main
   ```

2. Deploy to Vercel:
   - Go to [vercel.com](https://vercel.com)
   - Import your GitHub repository
   - Vercel will automatically detect the static site and deploy it

## Technologies

- HTML5
- CSS3 (with gradients and animations)
- Vanilla JavaScript (no frameworks required)

## License

MIT

